# ==============================================================================
# SparseMap Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.sample .env

# ==============================================================================
# LLM Configuration (Required)
# ==============================================================================
# Choose LLM Provider: "gemini" or "deepseek"
LLM_PROVIDER=gemini

# For Gemini (LLM_PROVIDER=gemini):
# Get your API key from: https://aistudio.google.com/app/apikey
LLM_API_KEY=your_gemini_api_key_here

# Available Gemini Models:
# - gemini-2.0-flash-exp       (Recommended) Fastest, latest, best for structured JSON output
# - gemini-1.5-flash           (Stable) Good balance of speed and quality
# - gemini-1.5-pro             (Advanced) Better reasoning, slower, more expensive
# - gemini-exp-1206            (Experimental) Latest features, may be unstable
LLM_MODEL=gemini-2.0-flash-exp

# For DeepSeek (LLM_PROVIDER=deepseek):
# Uncomment and set these when using DeepSeek
# LLM_API_KEY=your_deepseek_api_key_here
# LLM_BASE_URL=https://space.ai-builders.com/backend/v1
# LLM_MODEL=deepseek

# Optional LLM Parameters (uncomment to override defaults)
# LLM_TEMPERATURE=0.2          # Range: 0.0-1.0 (lower = more deterministic, higher = more creative)
# LLM_MAX_TOKENS=2000          # Maximum tokens in LLM response
# LLM_MAX_RETRIES=2            # Number of retry attempts on failure

# ==============================================================================
# Database Configuration (Required)
# ==============================================================================
# Format: postgresql+psycopg://username:password@host:port/database
# Make sure this matches your docker-compose.yml settings
DATABASE_URL=postgresql+psycopg://postgres:your_secure_password@localhost:5434/sparsemap_v1

# ==============================================================================
# Content Extractor Settings (Optional)
# ==============================================================================
# EXTRACTOR_MAX_CHARS=8000     # Maximum characters to extract from each source
# EXTRACTOR_MIN_CHARS=200      # Minimum characters required for valid content

# ==============================================================================
# Application Settings (Optional)
# ==============================================================================
# APP_NAME=SparseMap
# ENVIRONMENT=development       # Options: development, production

# ==============================================================================
# Extractor Configuration
# ==============================================================================
# Minimum characters required for URL content to be analyzed (default: 200)
EXTRACTOR_MIN_CHARS=50